name: flutter_agc_mockup
description: A new Flutter project.

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ">=0.6.13"

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ">=2.0.0"

flutter:
  uses-material-design: true

  assets:
    - assets/images/
